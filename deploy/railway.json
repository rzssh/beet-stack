{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/backend/Dockerfile"
  },
  "deploy": {
    "startCommand": "bun run --filter=backend start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
